Programa  Exercicio02Lista02;

Var 
    real: nota1, nota2, nota3, peso1, peso2, peso3, media;
    
Inicio

    imprima("Digite as notas e pesos: ");
    leia(nota1, nota2, nota3, peso1, peso2, peso3);
    media = ((nota1 * peso1) + (nota2 * peso2) + (nota3 * peso3)) 
                    / (peso1 + peso2 + peso3);

    imprima("Média: " + media);

Fim.